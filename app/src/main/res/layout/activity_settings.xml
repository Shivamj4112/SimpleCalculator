<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F8F8F8">

    <LinearLayout
        android:id="@+id/header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:padding="16dp"
        app:layout_constraintTop_toTopOf="parent">

        <ImageView
            android:id="@+id/btnBack"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:padding="12dp"
            android:src="@drawable/ic_back" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Settings"
            android:textSize="20sp"
            android:textColor="#000"
            android:layout_marginStart="8dp" />
    </LinearLayout>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/header"
        app:layout_constraintBottom_toBottomOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <!-- Personalization -->
            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:padding="16dp"
                android:text="Personalization"
                android:textColor="#888"
                android:textSize="14sp"
                android:textStyle="bold" />

            <include layout="@layout/item_settings_row" android:id="@+id/itemLanguage" />
            <include layout="@layout/item_settings_row" android:id="@+id/itemTheme" />
            <include layout="@layout/item_settings_row" android:id="@+id/itemScientific" />
            <include layout="@layout/item_settings_row" android:id="@+id/itemNumbering" />
            <include layout="@layout/item_settings_row" android:id="@+id/itemVibration" />
            <include layout="@layout/item_settings_row" android:id="@+id/itemRadians" />
            <include layout="@layout/item_settings_row" android:id="@+id/itemParentheses" />
            <include layout="@layout/item_settings_row" android:id="@+id/itemModulo" />
            <include layout="@layout/item_settings_row" android:id="@+id/itemLockScreen" />

            <!-- Formatting -->
            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:padding="16dp"
                android:text="Formatting"
                android:textColor="#888"
                android:textSize="14sp"
                android:textStyle="bold"
                android:layout_marginTop="8dp" />

            <include layout="@layout/item_settings_row" android:id="@+id/itemDecimalPlaces" />
            <include layout="@layout/item_settings_row" android:id="@+id/itemScientificNotation" />

            <!-- Advance -->
            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:padding="16dp"
                android:text="Advance"
                android:textColor="#888"
                android:textSize="14sp"
                android:textStyle="bold"
                android:layout_marginTop="8dp" />

            <include layout="@layout/item_settings_row" android:id="@+id/itemLongPressCopy" />
            <include layout="@layout/item_settings_row" android:id="@+id/itemKeepAwake" />

            <!-- Caller settings -->
            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:padding="16dp"
                android:text="Caller settings"
                android:textColor="#888"
                android:textSize="14sp"
                android:textStyle="bold"
                android:layout_marginTop="8dp" />

            <include layout="@layout/item_settings_row" android:id="@+id/itemCallerSettings" />

            <!-- Others -->
            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:padding="16dp"
                android:text="Others"
                android:textColor="#888"
                android:textSize="14sp"
                android:textStyle="bold"
                android:layout_marginTop="8dp" />

        </LinearLayout>
    </ScrollView>
</androidx.constraintlayout.widget.ConstraintLayout>
